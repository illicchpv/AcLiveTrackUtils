github 👉 https://github.com/illicchpv/AcLiveTrackUtils

- **Class1.cs**
    - Класс-заглушка, созданный по умолчанию при инициализации проекта. Обычно не содержит никакой логики и используется только как шаблон. В рабочем проекте часто удаляется или переименовывается.
- **ITPoint.cs**
    - Является интерфейсом/структурой точки отслеживания. Определяет основные свойства для GPS-данных: координаты, время, параметры сигнала и пр. Используется как базовая единица для GPS-логики.
- **LongTime.cs**
    - Класс утилит для работы с временными значениями в виде длинных чисел (long). Преобразует время в формат, удобный для хранения (например, количество секунд от определённой даты) и обратно.
- **RawFilePointer.cs**
    - Структура для хранения позиции в raw-файле: содержит номер файла и конкретную позицию внутри него, а также методы сравнения для сортировки. Помогает при навигации и обработке больших бинарных файлов без их полной загрузки в память.
- **RawProcessState.cs**
    - Класс для управления текущим состоянием обработки raw-файлов: позволяет сохранять и восстанавливать прогресс при работе с большими данными.
- **TPoint.cs**
    - Структура с полной информацией о GPS-точке:
        - `t_imei`: идентификатор устройства
        - `t_lat`, `t_lon`: координаты
        - `t_alt`: высота
        - `t_pow`: мощность сигнала
        - `t_vel`: скорость
        - `t_SecFrom2000`: время точки (секунды с 2000 года)
        - `t_ServSecFrom2000`: серверное время
    - Основная структура для хранения деталированной информации о каждой точке отслеживания.
- **Utils.cs**
    - Главный класс утилит. Содержит:
        - Методы логирования
        - Преобразование структур в массивы байт и обратно
        - Работу с файлами, сериализацию/десериализацию
        - Общие вспомогательные методы для всей библиотеки
- **UtilsRW.raw.cs**
    - Методы для чтения/записи raw-файлов (бинарный формат хранения GPS-данных).
        - Запись и чтение состояния обработки файлов
        - Методы для потоковой обработки данных с синхронизацией доступа через мьютексы
- **UtilsRW.trk.cs**
    - Методы для чтения/записи трек-файлов (TRK), работы с массивами точек:
        - `WriteITPointsArrToFile()`: запись массива точек в файл с поддержкой синхронизации
        - `ReadITPointsArrFromFile()`: чтение массива точек из файла с синхронизацией
        - `ReadByttesArrFromFile()`: чтение массива байт из файла
        - Методы преобразования данных для групповой обработки
        - Особенно актуально для многопроцессного доступа к трекам[^1]

**Итого:**
Библиотека строится вокруг обработки GPS-координат, поддерживает работу с бинарными и трек-файлами, 
а также синхронизированный доступ к ним из нескольких потоков и процессов. 
Основная цель — хранение и обработка информации о движении объектов в реальном времени 
с возможностью последующего анализа и синхронизации данных.[^1]
<span style="display:none">[^2]</span>

<div align="center">⁂</div>

[^1]: https://github.com/illicchpv/AcLiveTrackUtils/blob/main/UtilsRW.trk.cs

[^2]: https://github.com/illicchpv/AcLiveTrackUtils

